stages:
- name: Build
  steps:
  - runScriptConfig:
      image: azul/zulu-openjdk-alpine:11-jdk      
      # maven:3-jdk-7
      shellScript: |-
        pwd
        ls -la
        mvn package -X 
- name: Publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: app-app:${CICD_EXECUTION_SEQUENCE}
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml